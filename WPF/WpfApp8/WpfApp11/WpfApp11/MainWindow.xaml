<Window x:Class="WpfApp11.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp11"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" FontSize="26">

    <!--<Window.CommandBindings>
        <CommandBinding x:Name="HelpCommand" Command="Help" CanExecute="HelpCommand_CanExecute" Executed="HelpCommand_Executed"/>
    </Window.CommandBindings>

    <StackPanel>
        <Menu FontSize="26">
            <MenuItem Header="Help" Command="Help"/>
        </Menu>
        <TextBox Text="{Binding Text, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>
        <Button Width="100" Height="60" Content="Help" Command="Help"/>
    </StackPanel>-->

    <StackPanel>
        <Menu FontSize="26">
            <MenuItem Header="Help" Command="{Binding MessageCommand}"/>
        </Menu>
        <TextBox Text="{Binding Text, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>
        <Button Width="100" Height="60" Content="Send" Command="{Binding SendCommand}"/>
    </StackPanel>
</Window>
