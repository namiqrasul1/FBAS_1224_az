<Window x:Class="WpfApp11MVVM.Views.EditVIew"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp11MVVM.Views"
        mc:Ignorable="d"
          Title="MainWindow" Height="450" Width="800" FontSize="20" FontFamily="Tahoma">
    <StackPanel>
        <Label Foreground="DeepSkyBlue">Author</Label>
        <TextBox Foreground="HotPink" Text="{Binding Book.Author, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Foreground="DeepSkyBlue">Name</Label>
        <TextBox Foreground="HotPink" Text="{Binding Book.Name, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Foreground="DeepSkyBlue">Publish Year</Label>
        <TextBox Foreground="HotPink" Text="{Binding Book.PublishYear, UpdateSourceTrigger=PropertyChanged}"/>

    </StackPanel>
</Window>
